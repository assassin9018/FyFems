<Window x:Class="MyFemsWpfClient.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:uc="clr-namespace:MyFemsWpfClient.Controls"
        xmlns:f="clr-namespace:MyFemsWpfClient"
        mc:Ignorable="d"
        Title="MyFEMS - My free education message service"
        Icon="/Assets/AppIcon.png"
        WindowStartupLocation="CenterScreen"
        Height="480" Width="900"
        d:DesignHeight ="300" d:DesignWidth="600"
        x:Name="MasterWindow"
        DataContext="{Binding Source={StaticResource Locator}, Path=ApplicationViewModel}">
    <Window.Resources>
        <ResourceDictionary>

        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource gradientStyle}">
        <uc:AuthView DataContext="{Binding Source={StaticResource Locator}, Path=AuthVM}" Visibility="{Binding ElementName=MasterWindow, Path=DataContext.AuthVM.IsLoggedIn, Converter={StaticResource boolToVisibilityConverter}, ConverterParameter=inverse}"/>
        <uc:MainView DataContext="{Binding Source={StaticResource Locator}, Path=MainVM}" Visibility="{Binding ElementName=MasterWindow, Path=DataContext.AuthVM.IsLoggedIn, Converter={StaticResource boolToVisibilityConverter}}"/>
    </Grid>
</Window>
